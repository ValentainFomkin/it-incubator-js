/* Создайте ifоператор внутри функции для возврата Yes, that was true,
 если параметр wasThatTrueесть, trueи возврата в No, that was falseпротивном случае.*/
function trueOrFalse(wasThatTrue) {
  if (wasThatTrue) {
    return 'Yes, that was true';
  }
  return 'No, that was false';
}
